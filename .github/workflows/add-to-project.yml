name: Add to IS-Dev

on:
  issues:
    types: [opened]

jobs:
  add-to-project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v2.0.0
        with:
          project-url: https://github.com/users/IanMStevenson/projects/2
          github-token: ${{ secrets.PROJECT_TOKEN }}
